git config --global user.name "mason"
git config --global user.email "mason@gmail.com"

mklink /d "C:\Users\tangpeng\.lmstudio" "E:\Program Files\LLM_Models"

sha256sum Qwen3.5-27B

chrome://webrtc-internals/

./redis-server.exe redis.windows.conf

C:\users\tangpeng\appdata\local\temp\

docker run -it --name lusa -p 2222:22 ubuntu:24.1

docker commit 79c73b1635aa ubuntu:24.1

docker save -o ubuntu.24.1.tar ubuntu:24.1

ffmpeg -i 123.flv -ss 00:00:00 -t 00:05:00 -c copy output.mp4

ffmpeg -re -i sample.mp4 -c:v copy -c:a copy -f flv rtmp://172.20.173.132/live/test

ffmpeg -re -i EP45.mkv -c:v copy -c:a copy -f flv rtmp://172.20.173.132/live/test

ffplay http://172.20.173.132/live/test.live.flv

ffplay -rtsp_transport tcp rtsp://172.20.173.132/live/test

ffplay http://172.20.173.132/live/test/hls.m3u8

git clone https://github.com/emscripten-core/emsdk.git

ffplay -rtsp_transport tcp rtsp://172.20.173.132/live/camera1

rtsp://127.0.0.1/record/EP45x.mp4

rtsp://127.0.0.1/record/samplex.mp4

ffprobe -v error -select_streams v:0 -skip_frame nokey -show_entries \
 frame=best_effort_timestamp_time,coded_picture_number,pict_type -of csv=p=0 samplex.mp4

ffprobe -v error -select_streams v:0 -show_entries stream=r_frame_rate,avg_frame_rate  \
 -of default=noprint_wrappers=1 samplex.mp4

ffmpeg -hwaccel cuda -hwaccel_output_format cuda -i EP45.mkv -c:v h264_nvenc  \
 -preset p5 -tune hq   -rc vbr -cq 23 -b:v 0   -g 30 -forced-idr 1   -c:a aac   -movflags +faststart   output_seek.mp4

ffmpeg -hwaccel cuda -hwaccel_output_format cuda -i sample.mp4 -c:v hevc_nvenc \
  -preset p5 -tune hq   -rc vbr -cq 23 -b:v 0   -g 30 -forced-idr 1   -c:a aac   -movflags +faststart   output_seek_h265.mp4

go run . -addr :8080 -zlm http://127.0.0.1:18080 -tls-cert certs/dev.crt -tls-key certs/dev.key

requirepass "ZyeRVq3RKI9juNk"
bind 0.0.0.0
sudo systemctl restart redis

设置时区
timedatectl list-timezones | grep Shanghai
Asia/Shanghai
timedatectl set-timezone Asia/Shanghai

扩展EBS卷的文件系统
sudo lsblk
sudo growpart /dev/nvme0n1 1
sudo resize2fs /dev/nvme0n1p1

nginx命令
sudo nginx -s reload
sudo service nginx restart
chmod 755 -R /root/

mysql操作
sudo apt install mysql-server
sudo systemctl status mysql
mysql -u root -p
CREATE DATABASE rtc_room_server;
CREATE USER 'new_user'@'localhost' IDENTIFIED BY 'password';
GRANT ALL PRIVILEGES ON rtc_room_server.* TO 'new_user'@'localhost';

ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY '159357';
mysql> UNINSTALL COMPONENT 'file://component_validate_password';
SOURCE /root/roomserver/bin/rtc_room_server.sql

android-ndk编译
export ANDROID_NDK_HOME=/home/lusa/Android/android-ndk-r22
export PATH=$ANDROID_NDK_HOME:$PATH
cmake ..  -DANDROID_NDK=/home/lusa/Android/android-ndk-r22  -DANDROID_ABI=armeabi-v7a -DCMAKE_TOOLCHAIN_FILE=/home/lusa/Android/android-ndk-r22/build/cmake/android.toolchain.cmake
cmake ..  -DANDROID_NDK=/home/lusa/Android/android-ndk-r22  -DANDROID_ABI=arm64-v8a -DCMAKE_TOOLCHAIN_FILE=/home/lusa/Android/android-ndk-r22/build/cmake/android.toolchain.cmake

86415898  Next1-6!

PATH=$PATH:/etc/go/bin
PATH=$PATH:/etc/node/bin
source ~/.bashrc

X @pengtang155789

g4dn.xlarge --- t3.medium

nginx证书安装
sudo apt-get install certbot python3-certbot-nginx
sudo certbot --nginx
sudo certbot renew --dry-run
sudo certbot certificates

定时任务
crontab -e
0 0 * * * cp -rf /var/lib/redis/dump.rdb /etc/redis/
cp dump.rdb /var/lib/redis
crontab -l

SSH登录配置
systemctl restart ssh
PermitRootLogin yes
PasswordAuthentication yes
PubkeyAuthentication yes

Host ubuntu22.4
    HostName 192.168.163.141
    User root
    IdentityFile  ~/.ssh/id_rsa

大文件查找
du -h / --max-depth=1 | sort -hr | head -n 15

sudo sysctl -p
kernel.core_pattern = core
ulimit -c unlimited 

日志定位
log.SetFlags(log.Ldate | log.Ltime | log.Lshortfile)

双重身份验证
6fda5-00baa 5c9fa-10acf 998b0-288c8 2bb6a-1114d 4ae72-61e8b 9ca89-6fe7e 98c4e-fb045 da268-0eda7

intellisense配置
Verify IntelliSense is Enabled: Go to File > Preferences > Settings (or press Ctrl+), then search for "intellisense"
 Make sure that "C_Cpp: IntelliSense Engine" is set to "Default"

conda虚拟环境安装
wget -c https://repo.continuum.io/miniconda/Miniconda3-latest-Linux-x86_64.sh

FIR滤波
codegen  fir1 -args {99, [400 1000]/(44100/2), 'bandpass' , hamming(100), 'scale'}
codegen  -lang:c++ -config:lib fir1 -args {99, [400 1000]/(44100/2), 'bandpass' , hamming(100), 'scale'} -report
codegen  -lang:c++ -config:lib fir1 -args {99, [400 1000]/(44100/2)} -report
codegen  -lang:c++ -config:lib hamming -args {100} -report

动态数组
codegen myMultiply -args {x}
function y = myMultiply(a,b)
coder.varsize('y');
y = a:b;
end

函数编写
function y = bandpassFilter(a, b)
Fs = 48000;  % Sampling Frequency
N    = 99;       % Order
Fc1  = a;     % First Cutoff Frequency
Fc2  = b;    % Second Cutoff Frequency
flag = 'scale';  % Sampling Flag
win = hamming(N+1);
y  = fir1(N, [Fc1 Fc2]/(Fs/2), 'bandpass', win, flag);
end

编译工具链
setenv('MW_MINGW64_LOC', 'D:\mingw81');
mex -setup -v
mex -setup C++
g++ -shared -fPIC -o libexample.so *.cpp -fopenmp
g++ *.cpp -o demo -fopenmp
